apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-app
  labels:
    app: weather-app
spec:
  replicas: 4
  selector:
    matchLabels:
      app: weather-app
  template:
    metadata:
      labels:
        app: weather-app
    spec:
      containers:
      - name: weather-app
        image: krishna112/weather-app:v4
        ports:
        - containerPort: 5000

---

apiVersion: v1
kind: Service
metadata:
  name: weather-app
spec:
  type: LoadBalancer
  selector:
    app: weather-app
  ports:
    - port: 5000
      targetPort: 5000